- [ ] Update `components/Footer.tsx` layout to match the provided footer structure (keep same vector background images)
- [ ] Ensure social icons and ENFIQ watermark sizing/positioning match the provided snippet
- [ ] Run TypeScript build/lint (e.g., `npm run build` / `npm run lint`) to verify no errors

